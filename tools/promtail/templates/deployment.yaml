apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "promtail.fullname" . }}-deployment
  labels:
    {{- include "promtail.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "promtail.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "promtail.name" . }}
    spec:
      serviceAccountName: {{ .Values.rbac.serviceAccountName }}
      containers:
        - name: promtail
          image: grafana/promtail:2.9.0
          args:
            - -config.file=/etc/promtail/promtail.yaml
          volumeMounts:
            - name: config
              mountPath: /etc/promtail
            - name: varlog
              mountPath: /var/log
      volumes:
        - name: config
          configMap:
            name: {{ include "promtail.fullname" . }}-config
        - name: varlog
          hostPath:
            path: /var/log
